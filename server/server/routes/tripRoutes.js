const router=require('express').Router(); const {protect}=require('../middleware/authMiddleware'); const {createTrip,getTrips,updateTrip,deleteTrip,getExploreTrips,requestJoinTrip,respondJoinTrip,myTrips,getTripById}=require('../controllers/tripController'); router.get('/',protect,getTrips); router.get('/explore',protect,getExploreTrips); router.get('/mine',protect,myTrips); router.get('/:id',protect,getTripById); router.post('/',protect,createTrip); router.patch('/:id/request',protect,requestJoinTrip); router.patch('/:id/respond',protect,respondJoinTrip); router.patch('/:id',protect,updateTrip); router.delete('/:id',protect,deleteTrip); module.exports=router;
